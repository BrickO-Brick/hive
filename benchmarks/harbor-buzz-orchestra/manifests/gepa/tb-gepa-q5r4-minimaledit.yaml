# GEPA per-problem probe Q5 ROUND 4 (filter-js-from-html, 0/17 lifetime).
# Q5-r3 (hermetic rule) verdict 0/4 but the rule WORKED: zero import-crash
# deaths. Two remaining fronts, both parser-shaped: (a) 2/4 trials passed
# clean round-trip but left 1-2 XSS survivors — the repeat offender is the
# HTML5 comment-breakout `<!-->payload` (testbed #401): the agent's parser
# reads it as a comment, Chrome closes the comment and executes; the agent's
# own output scan used the same parser so the survivor was invisible to its
# gate. (b) 2/4 trials modified 3-5/12 clean files — tree-parse+reserialize
# mangling. Base: tb-gepa-q3-b lineage. Single variable vs Q5-r3: the
# defensive-deliverable paragraph rewritten — the artifact edits raw byte
# spans and never reserializes (kills the clean-file front), and the
# survivor scan is raw-byte and context-distrusting (a survivor counts even
# when a parser says it's inside a comment — attacks live in the parser/
# browser disagreement).
schema_version: "1"
condition: tb-gepa-q5r4-minimaledit
timing_signal: true
roster:
  - id: mia
    kind: orchestrator
    role: conn
    require_reply: true
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mia-v9-minimaledit.md
      sha256: b160fc5db71c2c062fb4502b2ce3236606e42de9dc7ead4def0742f901bd0e0d
    generation:
      thinking_effort: high
  - id: mie
    kind: worker
    role: twin
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mie-v0.md
      sha256: a4924e4c4507ac466a7500bb34a04a782618df41dba850b6f0f729787c48439b
    generation:
      thinking_effort: high
prices:
  deepseek-ai/Deepseek-V4-Flash:
    input_per_million_usd: 0.14
    cached_input_per_million_usd: 0.14
    output_per_million_usd: 0.28
    cache_read_rate: 0.0
trial_budget:
  timeout_seconds: 36000
