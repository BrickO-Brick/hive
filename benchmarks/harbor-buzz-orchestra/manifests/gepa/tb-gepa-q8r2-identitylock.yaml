# GEPA Q8 ROUND 2 (sam-cell-seg). r1 (alignmentlock) 3/6 — best sam-cell-seg
# arm in lineage (v0/v3: zero passes) but below the 4/6 bar. Misses: MttWQLy
# 8/9 died on test_no_polyline_overlaps by a whisker (5.84 vs 4.43 allowed);
# o4xUJuA 8/9 died on test_mask_alignment with ONE item at IoU exactly 0.0;
# kpLCmnC AgentTimeoutError. Wren's r2 call: target the IoU-zero, not the
# overlap whisker — an isolated 0.0 amid aligned items is categorical
# evidence of lost item correspondence or coordinate-frame drift, and a
# cleanup tweak for the overlap risks undoing the alignment behavior that
# produced the passes. Single variable vs r1: ONE identity/correspondence
# lock sentence group APPENDED to the alignment paragraph (stable item IDs,
# input order, coordinate frame, one-output-per-input through batching/
# postprocessing/serialization; validate final serialized artifact per item;
# isolated zero similarity = mapping/frame break to trace, never geometry
# cleanup). No overlap-specific erosion/suppression prose this round.
schema_version: "1"
condition: tb-gepa-q8r2-identitylock
timing_signal: true
roster:
  - id: mia
    kind: orchestrator
    role: conn
    require_reply: true
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mia-v17-identitylock.md
      sha256: 9f56c34162d46e1bc7e31677c68e75d8dbf4978267653dff6739a17e20e0a2cd
    generation:
      thinking_effort: high
  - id: mie
    kind: worker
    role: twin
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mie-v0.md
      sha256: a4924e4c4507ac466a7500bb34a04a782618df41dba850b6f0f729787c48439b
    generation:
      thinking_effort: high
prices:
  deepseek-ai/Deepseek-V4-Flash:
    input_per_million_usd: 0.14
    cached_input_per_million_usd: 0.14
    output_per_million_usd: 0.28
    cache_read_rate: 0.0
trial_budget:
  timeout_seconds: 36000
