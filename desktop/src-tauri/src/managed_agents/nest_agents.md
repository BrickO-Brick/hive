# Buzz Nest

This is the persistent workspace shared by your Buzz channel sessions. Work from this directory; do not search `$HOME` or `/` for another workspace.

- Repositories live under `REPOS/`. Before changing one, read its root and applicable path-local `AGENTS.md` files.
- Durable material belongs in `RESEARCH/`, `GUIDES/`, `PLANS/`, `WORK_LOGS/`, or `OUTBOX/`; temporary work belongs in `.scratch/`.
- Use the `buzz messages` command group directly for normal channel, thread, and DM conversation. Load the `buzz-cli` skill only for Buzz operations or messaging policy beyond that basic command group.
- Read `.agents/skills/buzz-cli/references/workspace.md` for knowledge-file conventions, and `references/repositories.md` before repository publication or commits.

<!-- BEGIN BUZZ MANAGED — regenerated automatically, do not edit below -->
## Active Agents

*(No agents deployed yet. Add agents in the Buzz desktop app.)*

## Workspace
- Relay: ws://localhost:3000
<!-- END BUZZ MANAGED -->
